package com.chatbot.whatsapp.entity.enums;

/**
 * Sentido de uma mensagem dentro de uma conversa.
 */
public enum MessageDirection {
    /** Mensagem enviada pelo cliente para o chatbot. */
    INBOUND,
    /** Mensagem enviada pelo chatbot para o cliente. */
    OUTBOUND
}
